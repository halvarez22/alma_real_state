{
  "name": "alma-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for ALMA Real State Portal",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0",
    "groq-sdk": "^1.2.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
